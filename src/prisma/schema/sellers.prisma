model sellers {
  seller_id    String   @id @default(uuid())
  name         String
  email        String   @unique
  password     String
  phone        String
  store_name   String
  created_at   DateTime @default(now())
  updated_at   DateTime @updatedAt

  clothes      clothes[]
  rentals      rentals[]
  escrow       escrow[]
}
